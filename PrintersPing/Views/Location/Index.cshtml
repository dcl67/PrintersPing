@model IEnumerable<PrintersPing.Models.Location>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Printer_Location)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Printer_Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Location_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Location_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Location_id })
        </td>
    </tr>
}

</table>
