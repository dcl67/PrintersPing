@model IEnumerable<PrintersPing.Models.Model>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Printer_Model)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Printer_Model)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Model_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Model_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Model_id })
        </td>
    </tr>
}

</table>
