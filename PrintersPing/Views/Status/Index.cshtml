@model IEnumerable<PrintersPing.Models.Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Printer_Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Printer_Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Status_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Status_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Status_id })
        </td>
    </tr>
}

</table>
